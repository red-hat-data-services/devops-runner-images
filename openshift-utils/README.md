# Openshift Utils 

The purpose of this image is to provide tools for dealing with openshift and OLM. 

It currently includes `oc`, `kubectl`, and `opm`, as well as all the binaries in the `base-runner` image
